#include "advent.h"
#include <iostream>
#include <fstream>

auto day06() -> int {
    std::string line;
    std::fstream inFile("input/day06.txt");
    assert(inFile.is_open());

//    while (getline(inFile, line)) {
//
//    }

    return 0;
}
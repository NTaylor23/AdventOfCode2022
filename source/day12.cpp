#include "advent.h"
#include <iostream>
#include <fstream>

auto day12() -> int {
    std::string line;
    std::fstream inFile("input/day12.txt");
    assert (inFile.is_open());

    std::cout << "Hello";

    return 0;
}
#include "advent.h"
#include "parser.h"
#include <iostream>
#include <unordered_map>
#include <utility>
#include <vector>
#include <variant>

#define NoValue LONG_MIN

auto day21b() -> int {
    std::string line;
    std::fstream inFile("input/day21.txt");
    assert (inFile.is_open());

    return 0;
}